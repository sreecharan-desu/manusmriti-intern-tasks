---
description: This repo is exactly three apps. Do not restore or add the removed ones.
alwaysApply: true
---

# Scope

This submission is exactly three independently runnable apps:

1. `backend/02-auth-workflow` — bcrypt + JWT API (port 8000) and React UI (port 5173)
2. `backend/12-file-upload` — JPEG/PNG magic-byte upload API (port 8002)
3. `ai/07-ticket-classifier` — prompt classifier with allowlist, lock, CLI + HTTP

Do not add, restore, or scaffold:

- `backend/01-inventory-api`
- `frontend/06-product-catalog`
- `frontend/07-job-board`

Do not add a seventh app. Do not train a model. Do not force-push `main`.
