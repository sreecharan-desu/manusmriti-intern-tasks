# Inventory
INVENTORY_DB_PATH=inventory.sqlite

# Auth
AUTH_JWT_SECRET=replace-me
AUTH_DB_PATH=auth.sqlite

# Uploads
UPLOAD_DB_PATH=uploads.sqlite
UPLOAD_STORAGE_DIR=storage

# Classifier (optional — offline fallback works without these)
GEMINI_API_KEY=
OPENAI_API_KEY=
